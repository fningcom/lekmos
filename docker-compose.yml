services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lekmos-php
    working_dir: /app
    volumes:
      - ./app:/app
    stdin_open: true
    tty: true
    command: ["php", "-a"]
